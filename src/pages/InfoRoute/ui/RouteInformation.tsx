import { Flex } from "antd";

import { InfoRoute } from "@/features/InfoRoute";

const RouteInformation = () => {
  return (
    <Flex style={{ maxWidth: "800px" }}>
      <InfoRoute />
    </Flex>
  );
};

export default RouteInformation;
